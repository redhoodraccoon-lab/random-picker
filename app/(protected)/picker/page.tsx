import { PickerApp } from "@/components/picker/PickerApp";

export const metadata = { title: "Draw — Drawlot" };

export default function PickerPage() {
  return (
    <div className="picker-page">
      <PickerApp />
    </div>
  );
}
